# 需求卡 REQ-MKT-13: A股交易额今日值获取失败修复（today 与昨日/序列独立容错）

> 🔒 **MANDATORY（强制门禁）**：本卡必须按状态机走完 ⬜→🔧→🧪→👀→✅，不可跳跃、不可省略。自测（🧪）是交付用户（👀）的前置条件；闭环（✅）需用户拍板。每轮必走，无例外。

- **状态**：🔧 实施中 → 🧪 自测中 → 👀 待用户验收
- **优先级**：P0
- **提出日期**：2026-07-16
- **提出人**：用户
- **关联阶段**：Phase 1.2
- **关联决策**：DEC-008/009
- **关联卡**：REQ-MKT-07、REQ-MKT-11、REQ-MKT-12

---

## 1. 需求描述
> 用户原话："怎么获取不到A股交易额了呢？"
> 理解：REQ-MKT-12 交付后，行情页 A股交易额卡片今日值获取不到（显示「—」），需修复今日成交额的稳定获取，且昨日/序列与今日同口径。

## 2. 验收标准（Acceptance Criteria）
- [ ] AC1：今日成交额稳定显示真实值（沪深两市合计，亿元），不依赖昨日/K线接口成败
- [ ] AC2：昨日成交额、近 20 日序列与今日同口径（均来自东财真实源，单位元→亿元）
- [ ] AC3：K线接口（/em-kline）偶发失败/慢时，今日值仍显示（不整体「—」）
- [ ] AC4：无运行时 JS 异常；卡片加载骨架 / 无数据「—」降级正常

## 3. 实施记录
### 🔧 实施中
- 日期：2026-07-16
- **根因**：REQ-MKT-12 把 `getQuote(今日 f48)` 与 `getKLines(昨日/序列)` 放在**同一个 try**；K线接口（push2his→Vite 代理 `/em-kline`）在用户本机偶发失败/慢时，整体抛异常 → catch → 返回 null → 整卡「—」。
- **纠正误判**：实测东财 push2 `stock/get` 的 `f48` **就是真实成交额（元）**，并非涨停价（此前假设错误）；`toSecid("000001","A-SH")="1.000001"` 正确。
- **修复**：`packages/data/src/providers/eastmoney.ts` 的 `getMarketTurnover()` 重构：
  - 今日（today）独立 `getQuote("000001"/"399001")` 取 `f48` 真实成交额（元），失败**不影响**昨日/序列；
  - 昨日 / 序列独立 `getKLines` 取真实 `amount`（元），失败**不影响**今日；
  - 双保险：今日实时接口未取到但 K线可用时，用 K线最后一根（当日）兜底；
  - 仅「今日与序列皆空」才降级「—」。
- 类型：`packages/data/src/types.ts` 的 `MarketTurnover.today` 放宽 `number | null`（今日可能获取失败）。

### 🧪 自测（我来做）
- **方法**：data 包 `tsc --noEmit` + 算法级 math 验证（mock K线数组）+ 无头 Chrome 抓 localhost:1420 运行时与 console
- **结果**：tsc 0 新增错误；math 全 PASS；无头零 `pageerror`；根因定位明晰
- **证据**：
  - `tsc --noEmit`：TSC_EXIT=0，无新增报错
  - math（/tmp/turnover_math_check.mjs）：今日 26000亿 / 昨日 24000亿 / delta +8.33% / 量级校正 26000 / 单根 / 空 全部 PASS
  - 无头 Chrome（puppeteer，连 localhost:1420）：`pageerror: 无`；确认 sandbox 下东财代理 `/em-quote`、`/em-kline` 全部 `500` 系 **sandbox Node 进程无外网**所致（大盘指数靠新浪/腾讯浏览器侧兜底仍显示 3882），**非代码 bug**；仅东财实现的方法（交易额/涨跌比/资金净流入）在 sandbox 下降级「—」
  - ⚠️ sandbox 无外网，vite 代理东财返回 500，故无头下 A股交易额/涨跌比/资金净流入均「—」；**真网数值需用户本机含网刷新确认**

### 👀 用户验收
- **结果**：待验收
- **操作**：请在本机含网环境重启 dev server（`pnpm dev`）并刷新 `localhost:1420`，确认：
  - A股交易额今日 ≈ 真实沪深成交额（亿元）、昨日 ≈ 昨日真实值、较昨日增减额/百分比正常；
  - 不再整体「—」（即使 K线接口偶发抖动，今日仍显示）；
  - A股涨跌比卡片正常（上涨/下跌/平盘家数 + 涨跌比）。

## 4. 闭环
- **结论**：待用户本机含网验收后回写 ✅
- **遗留 / follow-up**：若用户本机 `/em-quote` 或 `/em-kline` 代理仍 500，需查 Vite 代理配置或本地网络（与 sandbox 限制不同）
